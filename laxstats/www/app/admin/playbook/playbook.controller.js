(function() {
    'use strict';

    function playbookController($scope) {
        // Noop
    }

    angular.module('admin.playbook').controller('PlaybookCtrl', [ '$scope', playbookController ]);
})();