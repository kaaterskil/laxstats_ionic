(function() {
    'use strict';

    function scheduleController($scope) {
        // Noop
    }

    angular.module('main.schedule').controller('ScheduleCtrl', [ '$scope', scheduleController ]);
})();